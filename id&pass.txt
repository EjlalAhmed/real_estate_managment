admin id : admin@londonsquare.com
admin pass : admin123

Two Laravel and Vite
php artisan serve and npm rum dev

This is ENV file
APP_NAME=Laravel
APP_ENV=local
APP_KEY=base64:AN0+6m8D6qdpAR6keroKRXXfJsVfRAgKGmGRzmwqt08=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=london_square
DB_USERNAME=root
DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"






🏢 LONDON SQUARE – PROJECT FLOW & PRESENTATION
🎯 PROJECT KA PURPOSE

London Square ek property booking management system hai jisme:

Admin / Manager properties manage karta hai

Users apartments dekhte aur rooms book karte hain

Booking admin approval ke baad confirm hoti hai

Confirmed rooms date-wise hide ho jate hain (real-world logic)

👥 ROLES & RESPONSIBILITIES
👑 ADMIN

Admin system ka controller hai.

Admin kya kya kar sakta hai?

Dashboard dekhna (stats)

Apartments / Floors / Rooms ka CRUD

Users create karna

Roles & permissions assign karna

Bookings approve / cancel karna

Booking management

👉 Admin booking create nahi karta, admin approve karta hai

👤 USER / CUSTOMER

User system ka consumer hai.

User kya karta hai?

Login / Register

Apartments browse karta hai

Dates select karta hai

Available rooms book karta hai

Apni bookings dekhta hai

🧠 SYSTEM KA CORE IDEA

Room tabhi unavailable hota hai jab admin booking approve kare
aur sirf unhi dates ke liye

🔄 END-TO-END FLOW (STEP BY STEP)
1️⃣ LOGIN / REGISTER FLOW

User register karta hai → default role = user

Admin manually users create kar sakta hai

Roles & permissions Spatie se controlled
